# 🚀 Инструкция по запуску проекта

## 📋 Содержание

1. [Требования](#требования)
2. [Установка](#установка)
3. [Запуск на localhost](#запуск-на-localhost)
4. [Запуск для доступа с телефона](#запуск-для-доступа-с-телефона)
5. [Доступ к приложению](#доступ-к-приложению)
6. [Решение проблем](#решение-проблем)

---

## 📦 Требования

- Node.js (версия 16 или выше)
- npm или yarn
- mkcert (для HTTPS сертификатов) - опционально

---

## 🔧 Установка

### 1. Установка зависимостей Backend

```bash
cd backend
npm install
```

### 2. Установка зависимостей Mobile приложения

```bash
cd mobile
npm install
```

### 3. Установка зависимостей AR игр

```bash
cd mobile/ar-games
npm install
```

### 4. Установка mkcert (для HTTPS)

**Windows:**
1. Скачайте: https://github.com/FiloSottile/mkcert/releases
2. Скачайте `mkcert-v1.4.4-windows-amd64.exe`
3. Переименуйте в `mkcert.exe`
4. Скопируйте в `C:\Windows\System32`

**Установка корневого сертификата:**
```bash
mkcert -install
mkcert localhost 192.168.0.114
```

Сертификаты будут созданы в `%LOCALAPPDATA%\mkcert\`

---

## 💻 Запуск на localhost

### Вариант 1: Все серверы отдельно (для разработки)

**Терминал 1 - Backend:**
```bash
cd backend
node server.js
```
Backend будет доступен на: `http://localhost:5000`

**Терминал 2 - AR сервер:**
```bash
cd mobile/ar-games
npm run dev
```
AR сервер будет доступен на: `https://localhost:3001`

**Терминал 3 - Expo (основное приложение):**
```bash
cd mobile
npm run start:network
```
Expo будет доступен на: `http://localhost:8081`

**Терминал 4 - HTTPS прокси:**
```bash
cd mobile
npm run start:https
```
HTTPS прокси будет доступен на: `https://localhost:8443`

### Вариант 2: Все вместе (рекомендуется)

**Один терминал:**
```bash
cd mobile
npm run start:https:full
```

Эта команда запустит:
- Expo сервер (порт 8081)
- HTTPS прокси (порт 8443)

**Отдельно нужно запустить:**
- Backend: `cd backend && node server.js`
- AR сервер: `cd mobile/ar-games && npm run dev`

---

## 📱 Запуск для доступа с телефона

### Шаг 1: Узнайте IP адрес компьютера

**Windows:**
```powershell
ipconfig
```
Найдите IPv4 адрес (например: `192.168.0.114`)

**Linux/Mac:**
```bash
ifconfig
# или
ip addr
```

### Шаг 2: Запустите все серверы

**Терминал 1 - Backend:**
```bash
cd backend
node server.js
```

**Терминал 2 - AR сервер:**
```bash
cd mobile/ar-games
npm run dev
```

**Терминал 3 - Основное приложение и HTTPS прокси:**
```bash
cd mobile
npm run start:https:full
```

### Шаг 3: Откройте на телефоне

**На телефоне откройте в браузере:**
```
https://192.168.0.114:8443
```

**⚠️ Важно:** Замените `192.168.0.114` на IP адрес вашего компьютера!

### Шаг 4: Примите сертификат (первый раз)

При первом открытии браузер покажет предупреждение о сертификате:

**Android (Chrome/Яндекс Браузер):**
1. Нажмите **"Дополнительно"**
2. Нажмите **"Перейти на сайт (небезопасно)"**

**iOS (Safari):**
1. Нажмите **"Дополнительно"**
2. Нажмите **"Перейти на веб-сайт"**

После этого сертификат будет принят и приложение откроется.

---

## 🌐 Доступ к приложению

### На компьютере:

- **Основное приложение (HTTP):** `http://localhost:8081`
- **Основное приложение (HTTPS):** `https://localhost:8443`
- **AR игры (HTTPS):** `https://localhost:3001`
- **Backend API:** `http://localhost:5000/api`

### На телефоне (в той же Wi-Fi сети):

- **Основное приложение (HTTPS):** `https://192.168.0.114:8443`
- **AR игры (HTTPS):** `https://192.168.0.114:3001`
- **Backend API:** `http://192.168.0.114:5000/api`

**⚠️ Замените `192.168.0.114` на IP адрес вашего компьютера!**

---

## 🔍 Решение проблем

### Проблема: Приложение не открывается на телефоне

**Решение:**
1. Убедитесь, что телефон и компьютер в одной Wi-Fi сети
2. Проверьте, что IP адрес правильный
3. Проверьте, что все серверы запущены
4. Проверьте файрвол - порты 8443, 3001, 5000, 8081 должны быть открыты

### Проблема: Ошибка "Не защищено" или "ERR_CERT_AUTHORITY_INVALID"

**Решение:**
1. Примите сертификат в браузере (см. Шаг 4 выше)
2. Если не получается - откройте AR игры напрямую: `https://192.168.0.114:3001`
3. Примите сертификат там, затем вернитесь в основное приложение

### Проблема: AR игры не загружаются (белый экран)

**Решение:**
1. Убедитесь, что AR сервер запущен: `cd mobile/ar-games && npm run dev`
2. Проверьте, что AR сервер доступен: `https://localhost:3001`
3. Обновите страницу в браузере (F5)
4. Проверьте консоль браузера на ошибки

### Проблема: Не работает вход в аккаунт

**Решение:**
1. Убедитесь, что Backend запущен: `cd backend && node server.js`
2. Проверьте, что Backend доступен: `http://localhost:5000/api`
3. Проверьте консоль браузера на ошибки
4. Убедитесь, что используете HTTPS: `https://192.168.0.114:8443`

### Проблема: Камера не работает в AR играх

**Решение:**
1. Убедитесь, что используете HTTPS (не HTTP)
2. Примите сертификат (см. выше)
3. Разрешите доступ к камере в браузере
4. Проверьте настройки браузера - разрешения для камеры

### Проблема: mkcert не найден

**Решение:**
1. Установите mkcert (см. раздел "Установка")
2. Или используйте HTTP вместо HTTPS (но камера может не работать)

---

## 📝 Полезные команды

### Остановка всех серверов

**Windows PowerShell:**
```powershell
Get-Process node | Stop-Process -Force
```

**Linux/Mac:**
```bash
pkill node
```

### Проверка портов

**Windows:**
```powershell
netstat -ano | findstr ":8443"
netstat -ano | findstr ":3001"
netstat -ano | findstr ":5000"
netstat -ano | findstr ":8081"
```

**Linux/Mac:**
```bash
lsof -i :8443
lsof -i :3001
lsof -i :5000
lsof -i :8081
```

---

## 🎯 Быстрый старт

1. **Установите зависимости:**
   ```bash
   cd backend && npm install
   cd ../mobile && npm install
   cd ar-games && npm install
   ```

2. **Запустите все серверы:**
   ```bash
   # Терминал 1
   cd backend && node server.js
   
   # Терминал 2
   cd mobile/ar-games && npm run dev
   
   # Терминал 3
   cd mobile && npm run start:https:full
   ```

3. **Откройте на телефоне:**
   ```
   https://ВАШ_IP:8443
   ```

4. **Примите сертификат и готово!** ✅

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи серверов
3. Убедитесь, что все порты свободны
4. Проверьте, что все зависимости установлены

---

**Дата обновления:** 27.11.2025  
**Версия:** 1.0

